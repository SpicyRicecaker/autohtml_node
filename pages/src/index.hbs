<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>&nbsp;</title>
</head>

<body>
  <div style="position: relative; overflow: hidden; padding-top: 75%">
    <iframe style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: 0"
      src="{{pdfURL.value}}"></iframe>
  </div>
  <div style="
        font-size: 0.7rem;
        min-width: 400px;
        margin: 1rem auto;
        text-align: center;
      ">
    <a href="{{desmosURL.value}}" target="_blank">desmos</a>
    <a href="{{repoURL.value}}/disc-{{disc.value}}/disc-{{disc.value}}-{{topic.value}}/disc-{{disc.value}}-{{topic.value}}.html"
      target="_blank">html</a>
    <a href="{{repoURL.value}}/disc-{{disc.value}}/disc-{{disc.value}}-{{topic.value}}/disc-{{disc.value}}-{{topic.value}}.tex"
      target="_blank">tex</a>
    <a href="{{pdfURL.value}}" target="_blank">pdf</a>
  </div>
</body>

</html>